# MongoDB Configuration
MONGODB_URI=mongodb+srv://khuntakash1211_db_user:lZH3uGqPnScmNLJS@cluster0.ax0teyf.mongodb.net/growloan?retryWrites=true&w=majority

# JWT Secret Key (Change this to a strong random string in production)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Server Port
PORT=5000

# Environment
NODE_ENV=development

# Firebase Admin SDK Configuration
# Option 1: Download serviceAccountKey.json from Firebase Console and place in backend/config/
# Option 2: Use environment variables below

# Firebase Project ID
FIREBASE_PROJECT_ID=your-project-id

# Firebase Private Key ID
FIREBASE_PRIVATE_KEY_ID=your-private-key-id

# Firebase Private Key (Keep the \n characters, wrap in quotes)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate new private key
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Firebase Client Email (Service Account Email)
FIREBASE_CLIENT_EMAIL=your-service-account-email@your-project-id.iam.gserviceaccount.com

# Firebase Client ID
FIREBASE_CLIENT_ID=your-client-id

# Firebase Client X509 Cert URL
FIREBASE_CLIENT_X509_CERT_URL=https://www.googleapis.com/robot/v1/metadata/x509/your-service-account-email%40your-project-id.iam.gserviceaccount.com

# Instructions:
# 1. Copy this file to .env (rename env.example.txt to .env)
# 2. Replace all placeholder values with your actual credentials
# 3. For Firebase: Either use serviceAccountKey.json file OR fill in Firebase environment variables above
# 4. Never commit .env file to git (it's in .gitignore)

